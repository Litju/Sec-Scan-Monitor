# Local synthetic/default configuration only. Never commit a real value.
NEXT_PUBLIC_SECSCAN_MODE=PREVIEW
# Set these only for an explicitly configured local integration.
# SECSCAN_API_URL=http://127.0.0.1:8000
# SECSCAN_PRINCIPAL=local-operator
# SECSCAN_DB_URL=postgresql+psycopg://secscan:REPLACE_ME@127.0.0.1:5433/secscanmonitor

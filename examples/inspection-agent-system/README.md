# Synthetic inspection example

This example is synthetic and non-operational. It demonstrates the public evidence chain for an agent-built system named `Acme Agent Platform`.

The example contains no credential, personal data, client material, raw target bytes, or claim of a real-world zero-finding result. The expected conclusion is intentionally `INCONCLUSIVE` because one required authority reference is absent.
